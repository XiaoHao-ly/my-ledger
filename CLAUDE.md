# 房东记账 App · 设计文档

> **这份文档是写给「不会编程的房东」看的。**
> 里面没有一个字需要你懂技术。遇到看不懂的词，翻到最后的《小白词典》。
>
> **这份文档也是 AI 的开工说明书。** 每次开始工作前，AI 必须先读这份文档，
> 并按第 1 章的约定和你配合。任何和本文档冲突的做法，都以本文档为准。

**最后更新**：2026-09-10

---

## 第 0 章 · 这份文档是什么

这是「房东记账 App」的设计图纸。就像盖房子前先画图纸一样，
**先把「要盖成什么样」定下来，再动手盖**。

它解决三个问题：

1. **你要的东西，我理解对了吗？** —— 第 2、4、5 章。
2. **为什么选这个做法？** —— 第 3 章，含当初对比过的其他方案。
3. **以后怎么配合？** —— 第 1 章，这是最重要的一章。

**这份文档会跟着项目一起长大。** 以后每加一个功能、每改一个决定，
都会先改这份文档，再改代码。

---

## 第 1 章 · ⭐ 怎么跟我沟通（最高优先级）

> 这一章是房东特别要求的，**整个开发期间必须严格遵守**。
> 它的优先级高于本文档的其他所有内容。

### 1.1 永远说人话

**不许用**「部署」「接口」「字段」「实例」「渲染」「依赖」这类词。

必须用到某个技术词时，**当场用大白话解释一遍**。例如：

> ❌ 不好的说法：「我把 manifest 补上，这样就能被识别为 PWA 了。」
>
> ✅ 好的说法：「我加一个叫 manifest 的小文件。它是干嘛的呢——
> 手机看到这个文件，才知道『原来这是个 App，不是普通网页』，
> 才会允许你把图标放到桌面上。」

### 1.2 凡技术决定，必须先给方案让房东挑

**绝对不许自己拍板。** 至少给 2~3 个方案，每个都要讲清楚：

| 要讲清楚的 | 举例 |
|---|---|
| **是什么** | 「这个方案是把程序放到网上，用网址打开」 |
| **好处** | 「桌面上能有真图标，点开全屏，像 App」 |
| **坏处** | 「需要注册一个账号，大概 10 分钟」 |
| **花钱吗** | 「免费」 |
| **麻烦吗** | 「一次性设置，之后不用管」 |

讲完让房东选。**不能替他做决定，也不能用「推荐」两个字糊弄过去**——
要说清楚为什么推荐。

### 1.3 每次动手前先报备

动手改东西之前，先说清楚三件事：

1. **我要改什么**（大白话）
2. **改完你会看到什么变化**（在手机上具体能看到什么不同）
3. **有没有风险**（会不会影响已有数据、能不能撤销）

### 1.4 删数据、覆盖文件之前，必须单独确认一次

不管多小的数据，**删之前都要问一句**，并且说清楚后果。
例如：

> 「删除这个房间，它下面 18 条收租记录也会从统计里消失。
> 确定要删吗？（删了也能从回收站恢复，30 天内有效）」

### 1.5 不假设房东懂

宁可多解释一句，也不要默认他知道。
**看不懂不是他的问题，是解释得不够好。**

### 1.6 汇报进度用固定句式

每次做完一步，都用这个句式汇报：

> **「做完这一步，你现在能在手机上看到 ______。」**

不要用「已完成数据层接入」这种话。

### 1.7 不替房东做涉及他身份的操作

注册账号、实名认证、绑定手机号这类事，**只能带他做，不能替他做**。
（需要输入密码、验证码、身份证的步骤，一律由房东本人操作。）

### 1.8 房东说「不懂」的时候

不要重复一遍刚才的话。**换一个更简单的说法**，最好打个比方，
或者用他熟悉的东西（房子、租客、收租）来类比。

---

## 第 2 章 · 这个 App 是干什么的

### 2.1 一句话

**一个装在手机上的记账本，专门用来记住：哪间房租出去了、租给谁、租金多少、这个月收到没有。**

### 2.2 你平时怎么用它

```
  ① 建区域          ② 建房间           ③ 租出去          ④ 收租
 ┌─────────┐      ┌─────────┐       ┌─────────┐      ┌─────────┐
 │ 城东小区 │  →   │101 102  │   →   │101 张三  │  →   │点一下   │
 │ 3栋     │      │201 202  │       │1200/月  │      │「确认已收」│
 │ 老房子   │      │301 302  │       │3月1日起 │      │         │
 └─────────┘      └─────────┘       └─────────┘      └─────────┘
   建一次           一次建24间         登记租客          每月收钱时点一下

  ⑤ 看账
 ┌──────────────────────────┐
 │ 今年实际收到  ￥13,200    │
 │ 还有 1 间没收  ￥1,200    │
 │ 空置少赚的    ￥3,300    │
 └──────────────────────────┘
```

### 2.3 为什么不直接用纸笔或 Excel

- **不会漏收**：到了收租日还没确认，那间房自动冒出来提醒你。
- **算得清**：今年到底收了多少，一眼看到，不用翻本子加。
- **知道亏在哪**：房子空着少赚了多少钱，心里有数。
- **不怕丢**：能导出备份，换手机能搬过去。

---

## 第 3 章 · 技术方案

### 3.1 最终选定的方案

> **手机网页版 + 传到网上（GitHub Pages）**

**它是怎么工作的**：

```
   你的电脑                          你的手机
 ┌──────────────┐               ┌──────────────────┐
 │  程序文件     │   传上去 →    │  打开网址         │
 │  (index.html │               │  → 加到桌面        │
 │   等几个文件) │               │  → 变成一个 App    │
 └──────────────┘               └──────────────────┘
                                        │
                                   ┌────┴─────┐
                                   │ 你的记账数据 │
                                   │ 只存在手机里 │
                                   │ 永不上传    │
                                   └──────────┘
```

**关键点：上传的只是「程序」，不是你的数据。**

打个比方：
- 上传的 = **一台空的收银机**（谁打开都是空的）
- 你的数据 = **你收银机里记的账**（只有你自己这台手机上有）

别人打开同一个网址，看到的是一间空房子，里面什么都没有。

**好处**：
- ✅ 桌面上有真图标，点开全屏，和真正的 App 一模一样
- ✅ 不用花钱，不用买服务器
- ✅ 断网也能用（记账不需要联网）
- ✅ 以后我改了功能，你刷新一下就是新版，不用重装
- ✅ 数据存在你自己手机上，连我都看不到

**坏处**：
- ❌ 需要注册一个 GitHub 账号（免费、不实名、不绑手机号，约 10 分钟）—— **你已经建好了**
- ❌ GitHub 在国内访问**有时**会不稳定（第 9 章写了备用方案）
- ❌ 只能自己用，不能分享给别人一起记

### 3.2 当初对比过的其他方案（留档，避免以后反复讨论）

| | **手机网页版**（已选） | 微信小程序（纯本地） | 微信小程序 + 云端 |
|---|---|---|---|
| 要注册账号吗 | GitHub 账号（已建好） | 小程序账号 + 实名认证 | 小程序账号 + 实名 + 云开发 |
| 花钱吗 | 免费 | 免费 | 超出免费额度要付费 |
| 能做桌面图标吗 | ✅ 能，真图标 | ✅ 能（在微信里） | ✅ 能（在微信里） |
| 断网能用吗 | ✅ 能 | ✅ 能 | ❌ 不能 |
| 数据存在哪 | **你的手机里** | 你的微信里 | 腾讯的服务器上 |
| 数据有容量限制吗 | 没有实际影响 | 有，10MB，很紧张 | 没有 |
| 换手机数据会丢吗 | 会（需先备份） | 会（需先备份） | 不会 |
| 开发难度 | 简单 | 中等 | 复杂 |
| 出问题好排查吗 | 好排查 | 一般 | 难，要登好几个后台 |

**为什么没选微信小程序**：
小程序必须注册账号并做实名认证，而且微信限制了本地存储只有 10MB，
照片基本存不了几张。功能上并不比网页版强，麻烦却多不少。

**为什么没选云端方案**：
需要联网才能用，超出免费额度要花钱，出问题要登好几个后台排查。
对一个「自己用」的记账本来说，这些都是不必要的负担。
而且数据放在别人服务器上，不如放自己手机上安心。

### 3.3 一个已查证的技术事实（解释了为什么不能只发个文件）

安卓系统有一条硬性规定：**本地文件（`file://`）不允许创建桌面图标，
也不允许离线缓存，存储容量也受限。**

这不是技术选择，是安卓系统的规定，绕不过去。所以「把文件发到手机直接打开」
这条路做不出真正的 App 图标。**这就是选择传到网上的原因**——只有通过网址打开，
安卓才允许你把它装到桌面上。

*（查证来源：Chromium 源码中明确对 `file://` 协议隐藏了「添加到主屏幕」功能；
且本地文件无法使用 Service Worker，也就没有离线能力。）*

---

## 第 4 章 · App 长什么样

> 下面是界面草图，用方框画的。实际做出来会比这个好看。

### 4.1 主界面

```
┌──────────────────────────────────────────┐
│  房东记账                          [统计] │
├──────────────────────────────────────────┤
│  城东小区3栋 │ 老房子 │  ＋区域           │ ← 第一层：区域
├──────────────────────────────────────────┤
│  全部   已租   未租   未交租 ②           │ ← 第二层：状态
├──────────────────────────────────────────┤
│ ┌──────────────────────────────────────┐ │
│ │ 101                  ●已租  ✓9月已收 │ │
│ │ 张三 · 138 1234 5678                 │ │
│ │ ￥1200/月         租期 2026-03-01 起 │ │
│ │                          [ 确认已收 ]│ │
│ └──────────────────────────────────────┘ │
│ ┌──────────────────────────────────────┐ │
│ │ 102                  ●已租  ⚠9月未收 │ │
│ │ 李四 · 139 8765 4321                 │ │
│ │ ￥1000/月         租期 2026-01-01 起 │ │
│ │                          [ 确认已收 ]│ │
│ └──────────────────────────────────────┘ │
│ ┌──────────────────────────────────────┐ │
│ │ 103                       ○未租      │ │
│ │ ￥900/月                             │ │
│ │                          [ 登记租客 ]│ │
│ └──────────────────────────────────────┘ │
└──────────────────────────────────────────┘
```

**颜色约定**（一眼看懂）：

| 标记 | 意思 | 颜色 |
|---|---|---|
| ✓ 已收 | 这个月钱收到了 | 绿色 |
| ⚠ 未收 | **到了收租日**还没收到（含当天） | 红色 |
| ● 已租 | 房间里住着人 | 蓝色 |
| ○ 未租 | 房间空着 | 灰色 |
| 🟠 租约已到期 | 租约到期了还没办退租 | 琥珀色 |

> **2026-09-10 改的**：原来还有个黄色的「🟡 今天该收租」，房东说多余 ——
> 到了收租日当天就该催，不该只是黄着。现在**收租日当天及以后一律算「⚠ 未收」**，
> 红标、进「未交租」列表、数字角标也跟着 +1。那个黄色的标记只留给「租约已到期」。

### 4.2 确认收租（点一下「确认已收」后弹出）

```
┌──────────────────────────────────────────┐
│                                          │
│      确认收到 101 房的租金               │
│                                          │
│    ┌────────────────────────────────┐    │
│    │  收租月份      2026年9月  ▾     │    │
│    │  租客          张三             │    │
│    │                                │    │
│    │  金额  [ 1200      ] 元         │    │
│    │        ↑ 默认填当月租金，        │    │
│    │          实际收到多少就改成多少  │    │
│    │                                │    │
│    │  收款日期  2026-09-03           │    │
│    ├────────────────────────────────┤    │
│    │ ✓ 这笔钱会算作 2026年9月 的收入  │    │ ← 实时跟着变
│    └────────────────────────────────┘    │
│                                          │
│        [ 取消 ]        [ 确认已收 ]      │
└──────────────────────────────────────────┘
```

**这个框是核心。** 只有在这里点过「确认已收」的钱，才会算进你的收入。

**⚠️「收租月份」和「收款日期」是两件事，别搞混：**

- **收租月份** = 这笔钱是**哪个月的租金** ← **决定这笔钱算进哪个月的收入**
- **收款日期** = 你**哪天**收到的钱（9月的租、10月3号才收到，是常有的事）

**2026-09-10 的教训（房东报的 bug）**：房东 9 月补记 7 月的租金，只把「收款日期」改成 7月28日，
「收租月份」还停在 9 月 → App 记成了「9月的租金、7月28日收到」→
**9 月被顶成「已收」，当月的收租提醒没了**，7、8 月还是红的，9 月的名额还被占住。

所以现在加了两道防线：

1. **月份跟着日期走**：只要房东没主动动过「收租月份」，改「收款日期」时月份自动跟过去
2. **实时预览 + 错配警示**：框底那行「这笔钱会算作 X 月的收入」随时更新；
   两个月份对不上时，跳一句人话 + 一个「改成 7 月」的按钮

**「收租月份」和「收款日期」不在同一个月是合法的**（跨月收款），
所以只在两边不一致时**提示**，不强制改。

### 4.2.1 补记以前月份的租金（★ 新增）

登记租客时，如果**起租日在以前**（比如 9 月 10 号才录入一个 7月28日入住的老租客），
App 会自动弹出这张清单：

```
┌──────────────────────────────────────────┐
│  101 · 以前的租金记一下                  │
├──────────────────────────────────────────┤
│  从 7月28日 入住到现在，这几个月的租金    │
│  收到了吗？收到就点一下，账就补上了。     │
│  9 月的收租提醒不受影响。                 │
│                                          │
│  2026年7月28日 · 700元  [收到了][没收到] │
│  2026年8月28日 · 700元  [收到了][没收到] │
│  2026年9月28日 · 700元   还没到收租日    │
└──────────────────────────────────────────┘
```

| 那一行的样子 | 什么意思 | 能做什么 |
|---|---|---|
| ⚠ **过了收租日、没记过** | 这笔钱该收了，账上还没有 | 点「收到了」→ 打开 4.2 那个确认框（月份、金额、日期都已填好）；点「没收到」这一行灰掉 |
| ✓ **已收 7月28日** | 已经记过账了 | 可以撤销 |
| 灰字 **还没到收租日（9月28日）** | 日子还没到，**不是欠租** | 什么都不用做 |

**为什么「收到了」还要再开一次确认框？** 因为金额可能不对（租客少给了钱）。
确认框里那一行「✓ 这笔钱会算作 2026年7月 的收入」正是补录时最该看清楚的东西。
记完一笔，**自动回到这张清单**接着补下一个月，不用重新点进来。

**几条要记住的规矩**：

- **「没收到」什么都不往账本里写。** 下次打开这张清单，它还会在——因为它确实还没收到。
  这不是 App 啰嗦，是它没撒谎。（同一次操作里它会灰掉，免得反复问。）
- **最多往回列 12 个月**，更早的不列（避免三年前的老租约刷出一长串）。
- **补录不会顶掉当月提醒。** 补完 7、8 月，9 月该收的时候照样提醒。
- 这张清单有三个入口：**登记租客（起租日在以前）时自动弹**、
  **房间详情页的「补记以前月份（N 个月没记）」按钮**、
  以及**「确认收租」框里的「还有 N 个月没记 → 去补录」**。

### 4.3 新建区域 / 批量建房

```
┌──────────────────────────────────────────┐
│  ← 新建区域                              │
├──────────────────────────────────────────┤
│  区域名称   [ 城东小区3栋             ]  │
│                                          │
│  房间怎么建？                            │
│    ● 批量建房（一次建多层多户）          │
│    ○ 只建一间                            │
│                                          │
│  从 [ 1 ] 层   到 [ 6 ] 层               │
│  每层 [ 4 ] 户                           │
│                                          │
│  房号怎么编？                            │
│    ● 层号+序号   → 101 102 103 104       │
│    ○ 纯序号      → 1 2 3 … 24            │
│    ○ 加个前缀 [A] → A101 A102 A103       │
│                                          │
│  统一月租金  [ 1200 ] 元   （可留空）    │
│  每月收租日  [ 5 ] 号                    │
│                                          │
│  ┌─── 将要创建 24 间房 ──────────────┐   │
│  │  101  102  103  104               │   │
│  │  201  202  203  204               │   │
│  │  301  302  303  304               │   │
│  │  401  402  403  404               │   │
│  │  501  502  503  504               │   │
│  │  601  602  603  604               │   │
│  └───────────────────────────────────┘   │
│                                          │
│           [ 确认创建 24 间 ]             │
└──────────────────────────────────────────┘
```

### 4.4 房间详情

```
┌──────────────────────────────────────────┐
│  ← 101                          [ 删除 ] │
├──────────────────────────────────────────┤
│  房号         [ 101           ]          │
│  月租金       [ 1200          ] 元       │
│  状态         ● 已租      ○ 未租         │
│  每月收租日   [ 5 ] 号   ⓘ小月会自动提前 │
│  押金         [ 2400          ] 元       │
├──────────────────────────────────────────┤
│  当前租客                                │
│  姓名         [ 张三          ]          │
│  电话         [ 13812345678   ]  📞点击拨打│
│  起租日       [ 2026-03-01    ]          │
│  租期到       [ 2027-02-28    ] （可留空）│
├──────────────────────────────────────────┤
│  这个房间的收租记录                      │
│    2026年9月   ￥1,200   ✓ 9月3日收      │
│    2026年8月   ￥1,200   ✓ 8月4日收      │
│    2026年7月   ￥1,000   ✓ 7月6日收      │
│                （7月少收了200元）         │
├──────────────────────────────────────────┤
│      [ 办退租 ]            [ 保存 ]      │
└──────────────────────────────────────────┘
```

> 草图比实际代码旧：实际没有「保存」按钮（改完立刻自动存），也没有「状态 ●已租 ○未租」
> 那个单选（房间是不是已租，由**有没有租约覆盖今天**自动决定，不用手选）。

### 4.4.1 登记租客（★ 2026-09-10 加了收租日）

```
┌──────────────────────────────────────────┐
│  登记租客 · 101                          │
├──────────────────────────────────────────┤
│  租客姓名     [ 张三                ]    │
│  电话         [ 13812345678         ]    │
│  起租日       [ 2026-07-28          ]    │
│  租期到       [                     ]    │
│  这个租客的月租金 [ 700 ] 元             │
│  押金         [ 700 ] 元                 │
│  每月几号收租（可留空） [ 27 ]           │  ← ★ 新增
│      ⓘ 和「房间信息」里那个是同一个      │
│  [ 登记 ]                                │
└──────────────────────────────────────────┘
```

**为什么要加**：房东说「录入租户信息时没有选择收租日期，我想有个输入的框」——
那个框原来只在**房间详情页**，录租客时看不到，老是忘了设。

**关键：两处是同一个值。** 它不是「这个租客的收租日」，就是**这间房的**
收租日（`room.rentDueDay`）。在租客表单里改了，房间详情页那个框也跟着变，反之亦然。

- 默认显示房间**现在**用的收租日；留空 = 用全局默认（设置页那个）
- 校验和提示跟房间详情页那个框完全一样：1~31 的整数，填错会弹提示并还原
- **不做任何推算**。房东明确说过「不需要计算天数」——
  不会根据起租日自动算出一个「前一天」之类的值，你自己填几号就是几号

### 4.5 统计页

```
┌──────────────────────────────────────────┐
│  ← 统计                    2026年 ▾      │
├──────────────────────────────────────────┤
│                                          │
│     2026 年实际收到                      │
│       ￥ 13,200                          │
│     只有你确认收过的钱才计入              │
│                                          │
├──────────────────────────────────────────┤
│     还有 1 间没收                        │
│       ￥ 1,200             [ 去处理 ]    │
│     只是提醒，不算收入                    │
│                                          │
├──────────────────────────────────────────┤
│     房子空着少赚的（估算）                │
│       ￥ 3,300                           │
│     不是支出，也不会从上面的钱里扣         │
│                                          │
├──────────────────────────────────────────┤
│     月份明细                             │
│     月份     收到       空置少赚         │
│     9月     ￥1,200     ￥333            │
│     8月     ￥1,200     ￥0              │
│     7月     ￥1,000     ￥200            │
│     6月     ￥1,200     ￥0              │
│     …                                    │
└──────────────────────────────────────────┘
```

**⚠️ 这三个数字互相独立，不能相加也不能相减。** 详见第 7 章。

---

## 第 5 章 · 完整功能清单

### 5.1 第一期要做的

| # | 功能 | 说明 | 对应你的要求 |
|---|---|---|---|
| 1 | **区域管理** | 新建/改名/删除区域，可排序 | 2.1 一级 |
| 2 | **房间列表** | 两层标签：先选区域，再选状态 | 2.1 二级 |
| 3 | **四种状态筛选** | 全部 / 已租 / 未租 / 当月未交租 | 2.1 二级 |
| 4 | **房间详情编辑** | 房号、月租金、入住情况、押金 | 2.2 |
| 5 | **租客信息** | 姓名、电话（点击可拨打） | — |
| 6 | **租期起止** | 起租日 + 租期到（可留空） | — |
| 7 | **批量建房** | 楼层范围 × 每层户数，自动编号 | 2.3a |
| 8 | **单户建房** | 只建一间 | 2.3b |
| 9 | **确认收租** | 点一下，金额可改 | ★ 核心 |
| 10 | **收租日提醒** | 过了收租日未确认 → 自动进「未交租」 | 2.1 / 2.2 |
| 11 | **收入统计** | 月度 / 年度实际收到的钱 | 2.4 |
| 12 | **空置损失** | 房子空着少赚的（单独一块） | 2.4 |
| 13 | **换租客 / 退租 / 续租** | 历史不覆盖 | — |
| 14 | **导出备份** | 一个文件 = 全部数据（给 App 恢复用） | — |
| 15 | **导入恢复** | 换手机、误删后恢复 | — |
| 16 | **回收站** | 误删的房间/租客能救回来 | — |
| 17 | **导出 Excel 表格** | 生成真 Excel 文件，**不打开 App 也能看账** | ★ 新增 |
| 18 | **补录以前月份** | 老租客的租金逐月补记，**不顶掉当月提醒** | ★ 新增（见 4.2.1） |
| 19 | **手动刷新** | 顶栏「↻」按一下，用现在的时间重算所有房间状态 | ★ 新增 |
| 20 | **录租客时设收租日** | 登记租客的表单里直接填「每月几号收租」，和房间信息里那个是同一个值 | ★ 新增（见 4.4.1） |

### 5.2 两种导出，别搞混

| | **导出 Excel 表格** | **导出备份** |
|---|---|---|
| **给谁用** | 给人看 | 给 App 用 |
| **长什么样** | 真 Excel 文件（.xlsx），打开就是表格 | 一个数据文件，打开是乱码，正常 |
| **干什么用** | 查看、打印、存档、发给别人 | 换手机、误删后恢复 |
| **改了有用吗** | 没用，只是一份记录 | 没用，只在恢复时才读 |
| **电脑上能打开吗** | ✅ 能，就是表格 | ❌ 不用打开 |

**记账的原始数据始终在 App 里。** 表格是「从账本上复印下来的一页」，
改复印件不会影响账本。详见第 9 章。

### 5.3 明确**不做**的功能

| 功能 | 为什么不做 |
|---|---|
| **房间照片** | **你明确要求去掉。** 去掉之后所有数据能装进同一个备份文件，备份更完整、更省心。 |
| 面积、备注 | 你选择不要这两个字段 |
| 数据图表（折线图、饼图） | 手机屏幕小，图表看不懂。用「大数字 + 一句人话」代替 |
| 多人共用 / 分享 | 目前是你自己用 |
| 提醒推送 | 需要联网和服务器，会牵出一堆麻烦。改用「打开 App 就看到」 |

### 5.4 以后可以加的（先不做，用顺手了再说）

- 收据打印 / 分享给租客
- 水电费、物业费记录
- 押金退还记录
- 按房间看历史走势

---

## 第 6 章 · 数据怎么存

> 这一章讲的是「你记的账，在手机里长什么样」。
> 你不需要看懂细节，但看懂之后你会更放心。

### 6.1 五样东西

手机里存着五本「账本」：

| 账本名字 | 存什么 | 打个比方 |
|---|---|---|
| **区域** | 城东小区3栋、老房子…… | 一个文件夹 |
| **房间** | 101、102……每间房的基本信息 | 文件夹里的卡片 |
| **租约** | 谁、从什么时候租到什么时候、租金多少 | 贴在卡片上的合同 |
| **收款记录** | 每一次你点「确认已收」 | 收据存根 |
| **设置** | 默认收租日、统计起始月等 | 你的偏好 |

### 6.2 为什么一个房间能存好几份「租约」

假设 101 房：

```
  2024-01-01 ────── 2025-12-31     张三    月租 1000
  2026-01-01 ────── 2026-06-30     李四    月租 1100
  2026-08-01 ────── （还在租）      王五    月租 1200
                 ↑
            中间 7 月空着
```

每一段都是一份**独立的租约**。换租客不会覆盖上一份，
所以「去年张三租的时候收了多少」永远查得到。

**如果只存一个「当前租客」字段会怎样？** 换租客的一瞬间，
张三的记录就被李四盖掉了，去年的账再也对不上。
所以必须分开存。

### 6.3 最重要的一件事：「未交租」是**算出来的**，不是存出来的

这一点决定了这个 App 会不会算错账，请务必理解。

**❌ 错误的做法**：每间房每个月提前生成一条「未交租」的空白记录。
> 后果：你要是三个月没打开 App，一打开就会凭空冒出几十条「欠租」，
> 而其中很多月份房子根本没租出去。你的账就全乱了。

**✅ 正确的做法**：手机里**只记录你真的点过「确认已收」的那些月份**。
「未交租」是打开 App 的时候，现场比出来的：

```
   这间房这个月租出去了吗？
        │
        ├─ 没有 ──→ 显示「未租」（不提收租的事）
        │
        └─ 有 ──→ 有我点过「确认已收」的记录吗？
                    │
                    ├─ 有 ──→ 显示「✓ 已收」，钱算进收入
                    │
                    └─ 没有 ──→ 显示「⚠ 未收」，进未交租列表
                                 （只是提醒，不算收入）
```

**好处**：不管你多久没打开 App，打开后看到的永远是正确的。
没有任何「定时重置」会失败。**你没有做过的操作，手机里就不会有记录。**

### 6.4 手机里大概占多大空间

去掉照片之后，数据非常小：

| 你有多少房 | 用了多少年 | 大约占用 |
|---|---|---|
| 20 间 | 5 年 | 不到 1 MB |
| 50 间 | 10 年 | 约 2 MB |
| 100 间 | 20 年 | 约 5 MB |

一部手机随便都是几十 GB 空间，**这个 App 占的地方可以忽略不计**。

---

## 第 7 章 · 钱怎么算

> **这一章是你的账本能不能信的关键。** 请重点看。

### 7.1 核心原则

> **只有你在「确认收租」框里点过确认的钱，才算你的收入。**
> 没确认过的，无论房间租给了谁、租金多少，**都不算**。

### 7.2 三个数字（⚠️ 互相独立，不能加减）

统计页上有三个数字。它们**回答的是三个完全不同的问题**，
放在一起只是方便你看，**千万不要把它们相加或相减**。

#### 数字一：实际收到 ← **这才是你的收入**

```
今年实际收到 = 今年所有「确认收款记录」的金额，一条一条加起来
```

**举例**：
```
  1月 101房 确认收到 1200
  1月 102房 确认收到 1000
  2月 101房 确认收到 1200
  2月 102房 确认收到  800   ← 租客少给了200，你如实填了800
  ─────────────────────────
  今年实际收到 = 4200
```

**这就是你真正拿到手的钱。** 一个字都不会多，一个字都不会少。

#### 数字二：还有几间没收 ← **只是提醒，不是收入**

```
这个月在租的房间中，你还没点「确认已收」的那些
```

显示时会把它们的月租金加起来给你看个参考数，但那**不是收入**，
只是让你知道「大概还有多少钱在外面没收回来」。

**举例**：3 间房在租，你确认了 2 间，第 3 间月租 1000 没确认。
→ 显示「还有 1 间没收 ￥1,000」。

这 1000 **不会**算进数字一。等你真收到了、点了确认，它才会变成收入。

#### 数字三：房子空着少赚的 ← **估算，不是支出**

```
某间房空着的天数 ÷ 当月总天数 × 这间房的月租金
（只算到今天为止，未来的日子不算）
```

**举例**：101 房月租 1200，9 月 20 日才租出去（前 19 天空着）。
9 月共 30 天。
```
  空置少赚 = 1200 ÷ 30 × 19 = 760 元
```

这是「本来能赚到、但没赚到」的钱，属于**机会成本**。

**⚠️ 三个重要说明**：
1. 它**不是你的支出**——你没有真的掏 760 块钱出去。
2. 它**不会从数字一里扣**——你的收入还是你实际收到的那些。
3. 它**只算到今天**——9 月刚过到 10 号，就只算到 10 号，不会预言未来。

### 7.3 为什么空置要按天算，不按整月算

假设 101 房月租 1200，9 月 20 日租出去了。

**❌ 如果按整月算**：9 月空置损失 = 1200（整整一个月）
然后 9 月又有 1200 的收入。
→ 你会看到「这个月赚了 1200，同时亏了 1200」，**这说不通，你会不再信任这个数字**。

**✅ 按天算**：9 月空置损失 = 1200 ÷ 30 × 19 = 760
→ 「这个月收到 1200，前 19 天房子空着，少赚了 760」——**说得通**。

### 7.4 各种情况下怎么算（边界规则）

| 情况 | 怎么处理 |
|---|---|
| **收租日设了 31 号，但 9 月只有 30 天** | 自动提前到 9 月 30 日。2 月自动变成 28（或闰年 29）日 |
| **新租客当月入住**（8月20日入住、收租日5号） | 8 月的提醒日按 8 月 20 日算，8 月 5 日**不会**误报「未交租」 |
| **到了收租日当天**（★ 2026-09-10 改） | 当天就**直接算「⚠ 未收」**：红标、进「未交租」列表、数字角标 +1。原来只显示黄标「今天该收租」，房东说多余 |
| **登记租客时能直接设收租日**（★ 新增） | 表单最下面那个框，和房间详情页那个是**同一个值**。默认显示房间现在用的日子，不做任何推算 |
| **刚装 App，填了老早以前的起租日** | 有个「统计起始月」，默认从你开始用的那个月算起。**不会凭空算出一堆历史欠租** |
| **同一个月内换了租客** | 那个月只算一次；租金按该月最后一份租约的租金 |
| **租约到期了，但你没点「办退租」** | **继续算在租**。（少算钱比多算钱更容易让人觉得算错了；到期了 App 会提醒你去处理） |
| **换租客中间空了一阵** | 空的那几天计入空置损失 |
| **租客少给了钱** | 确认收租时把金额改成实际收到的数。差额会显示在备注里 |
| **点了「确认已收」发现点错了** | 可以撤销（会有二次确认），撤销后统计立刻跟着变 |
| **9 月才录入一个 7月28日入住的老租客** | 弹出「以前的租金记一下」清单，逐月补录。**补录记的是哪个「月」，由你选**，不会顶掉 9 月的提醒（见 4.2.1） |
| **补记时只改了「收款日期」没改「收租月份」** | App 会自动把月份跟过去，并在框底实时显示「这笔钱会算作 X 月的收入」；两边对不上时跳警示 + 一键改（见 4.2） |

---

## 第 8 章 · 数据安全与备份

> **这一章请你一定要看完。** 你的账本只存在这台手机上，
> 手机丢了、清了浏览器数据，账就没了。

### 8.1 你的数据存在哪里

```
   ┌─────────────────────────────────────┐
   │         你的手机浏览器                │
   │  ┌───────────────────────────────┐  │
   │  │   房东记账 App 的数据          │  │
   │  │   （房间、租客、每一笔收款）    │  │
   │  └───────────────────────────────┘  │
   │                                     │
   │   ✗ 不上传到任何服务器               │
   │   ✗ 不同步到云端                     │
   │   ✗ 我（AI）也看不到                 │
   └─────────────────────────────────────┘
```

**好处**：绝对私密，没人能偷看。
**代价**：**手机坏了、丢了、清了浏览器数据，数据就没了。**

### 8.2 ⚠️ 什么情况会弄丢数据

| 会丢 | 不会丢 |
|---|---|
| 手机丢了 / 坏了 | 关掉 App |
| 清理浏览器数据 / 存储空间 | 手机重启 |
| 卸载浏览器 | 断网 |
| 在浏览器里点「清除浏览数据」 | App 更新版本 |
| 用「无痕模式」打开（数据存不下） | 换 Wi-Fi |

### 8.3 备份怎么做（请每月做一次）

**导出备份**：
```
1. 打开 App → 点右上角齿轮 ⚙（设置）
2. 点「导出备份」
3. 手机会下载一个文件，名字类似 房东记账备份-2026-09-10.json
4. 把这个文件发到「微信文件传输助手」或自己的邮箱保存
```

**这个文件里就是你的全部数据**——因为去掉了照片，
房间、租客、每一笔收款记录全都在这一个文件里。

**恢复备份**：
```
1. 打开 App → 点右上角齿轮 ⚙（设置）
2. 点「从备份恢复」
3. 选中之前保存的那个文件
4. App 会先把「你现在的数据」自动导出一份（防止误操作）
5. 确认后，数据就回来了
```

### 8.4 换手机怎么搬

```
   旧手机                          新手机
 ┌──────────┐                  ┌──────────┐
 │ 导出备份  │ ──微信/邮箱──→  │ 打开网址  │
 │ 得到一个  │                  │ 装成 App  │
 │ 文件      │                  │    ↓      │
 └──────────┘                  │ 导入备份  │
                               │ 数据全回来│
                               └──────────┘
```

**顺序很重要**：一定**先在旧手机上导出**，再在新手机上导入。
旧手机上的数据在你确认新手机没问题之前，**不要删**。

### 8.5 App 会主动提醒你备份

- 超过 30 天没备份 → 首页顶部会出现一条温和的提示：「已经一个月没备份了，点这里备份」
- 导出的文件名里带日期，方便你分辨新旧

### 8.6 误删了怎么办

- 删除房间、租客，都是**先放进回收站**，不是真删
- 回收站里的东西 **30 天内**可以随时恢复
- 设置页里有「回收站」入口

### 8.7 「清空全部数据」是危险操作

设置页最底下有个「清空全部数据」。点它需要：
1. 先自动导出一份备份
2. 手动输入「清空」两个字
3. 再确认一次

**三重保险，防止手滑。**

---

## 第 9 章 · 导出 Excel 表格

> 这一章解决你的问题：**「我不打开这个 App，也能看到我的数据吗？」**
> **能。** 导出来就是真 Excel 文件，电脑手机都能打开。

### 9.1 先搞清楚一件事：表格是「复印件」，不是「账本」

```
   ┌─────────────────────────┐        ┌─────────────────────────┐
   │   App 里的账本（原件）    │        │   导出的 Excel（复印件）  │
   ├─────────────────────────┤        ├─────────────────────────┤
   │  ✅ 能改                 │        │  ✅ 能看                 │
   │  ✅ 能算                 │        │  ✅ 能打印               │
   │  ✅ 能提醒               │  ───→  │  ✅ 能发给别人            │
   │  ✅ 算收入               │  复印   │  ❌ 改它不影响账本        │
   └─────────────────────────┘        └─────────────────────────┘
```

**为什么不能反过来（拿表格当账本）？**

因为浏览器有一条安全规定：**网页不能偷偷读写你手机里的文件**，
必须你每次手动去选一次文件。如果拿表格当账本，你每打开一次 App
就要选一次文件——太折腾，而且选错文件就可能把账搞乱。

**但是**，你想要的「数据能拿出来自己看」这件事，完全做到了。
而且这还是个**保命功能**：万一哪天 App 打不开、手机出问题，
你手里的 Excel 还在。

### 9.2 导出来长什么样

点「导出表格」后，你会得到一个**压缩包**。解压开是这样：

```
房东记账表格/
└── 2026/
    ├── 2026-城东小区3栋.xlsx
    └── 2026-老房子.xlsx
```

**为什么是压缩包？** 因为安卓不允许网页自己决定把文件存到哪个文件夹。
把文件夹「包」进压缩包里，解压出来就是你要的目录结构。

**解压方法**（安卓手机）：在「文件管理」里找到下载的压缩包 → 长按 → 选「解压」。

### 9.3 打开 Excel 后长什么样

一个文件 = **一个区域的一整年**，里面 **12 个标签页**，一个月一个。

```
   文件：2026-城东小区3栋.xlsx

┌──────────────────────────────────────────────────────────────┐
│  城东小区3栋 · 2026年9月                                      │
├──────┬──────┬──────┬──────┬──────┬──────────┬──────────┬─────┤
│ 房号 │ 区域 │ 月租 │ 押金 │ 租客 │ 入住时间 │ 退租时间 │ 状态│
├──────┼──────┼──────┼──────┼──────┼──────────┼──────────┼─────┤
│ 101  │城东3栋│ 1200 │ 2400 │ 张三 │2026-03-01│          │ 已收│
│ 102  │城东3栋│ 1000 │ 2000 │ 李四 │2026-01-01│          │ 未收│
│ 103  │城东3栋│  900 │      │      │          │          │ 空置│
│ 104  │城东3栋│  900 │ 1800 │ 王五 │2026-05-01│2026-09-15│ 已收│
└──────┴──────┴──────┴──────┴──────┴──────────┴──────────┴─────┘
   …（还有 本月实收、收款日期 两列）

 1月│2月│3月│4月│5月│6月│7月│8月│9月│10月│11月│12月
                          ↑ 底部标签，点一下就切到那个月
```

**每一列的意思**：

| 列 | 说明 |
|---|---|
| 房号 | 101、102…… |
| 区域 | 城东小区3栋（方便你把几个区域的文件合起来看） |
| 月租 | 这间房这个月的月租金 |
| 押金 | 收了多少押金 |
| 租客 | 这个月谁在住 |
| 电话 | 租客电话 |
| 入住时间 | 这个租客的起租日 |
| 退租时间 | 这个租客的退租日（还在租就是空的） |
| **本月状态** | 已收 / 未收 / 空置 |
| **本月实收** | 实际收到多少钱（租客少给钱时，这里和月租不一样） |
| **收款日期** | 哪天收到的 |

### 9.3.1 颜色（一眼看出哪行是什么情况）

**不用逐行读文字，看颜色就知道。**

| 颜色 | 意思 | 说明 |
|---|---|---|
| **白底** | 已收 | 正常在租、钱也收到了。不抢眼 |
| **浅红底** | 未收 | 在租但没收到钱。**需要你去看一眼** |
| **浅灰底 + 灰字** | 空房 | 房子空着。整行淡下去，不干扰 |
| **淡灰字（白底）** | 未到 | 还没到的月份，不瞎猜 |

「本月状态」那一格颜色更重（绿字浅绿底 / 红字浅红底 / 灰字灰底），
比整行更抢眼。标题行右边有一句图例说明，**打印出来也看得懂**。

**空房的几个特别处理**（避免看着像"数据丢了"）：

- **押金显示空白** —— 没租出去就没有押金，写个数字会让人以为钱押在手上
- **租客、电话、入住时间、退租时间显示「—」** —— 明确表示"不适用"，而不是漏填
- **月租照常显示** —— 这样你能看出"这间房本该收多少"
- **本月实收保持真空** —— 金额列不能填「—」，否则你在 Excel 里求和会出错

### 9.3.2 颜色改动记

**2026-09-10 房东反馈**：「空房的数据和有租户的数据并未区分开来，也不明显，
我第一眼以为数据有问题。」→ 于是加了上面的整行配色。

改颜色只需要动 `export.js` 里的 `ROW_STYLE` 和 `STATUS_STYLE` 两张表，
以及上面 `XLSX.styles` 里对应的字体/底色定义（**两处的编号必须对得上**）。

### 9.4 怎么导出

```
1. 打开 App → 点右上角齿轮 ⚙（设置）
2. 点「导出 Excel 表格」
3. 选择范围：
     ○ 今年
     ○ 去年
     ○ 全部年份
4. 点「导出」
5. 手机会下载一个压缩包
6. 在「文件管理」里解压，就能看到 年份/区域.xlsx
```

### 9.5 什么时候该导出

| 场景 | 建议 |
|---|---|
| **每月收完租** | 导出一次，当作这个月的存档 |
| **年底** | 导出全年，打印出来存档 |
| **要拿给家人/会计看** | 随时导出 |
| **换了新手机** | 导出**备份**（注意：是备份，不是表格，见第 8 章） |

### 9.6 ⚠️ 三个要注意的地方

**1. 表格是「那一刻的样子」，不会自己更新**

你今天导出，表格里就是今天的数据。
明天在 App 里改了租金，**昨天导出的那个文件不会跟着变**。
想知道最新的，重新导一次。

**2. 导表格 ≠ 备份**

- **表格**：给你看的，**不能**用来恢复数据
- **备份**：给 App 用的，**能**用来恢复数据
- **两个都要导**（换手机时尤其重要）

**3. 表格里没有「收租历史」**

表格是「每个月的快照」，体现的是那个月的情况。
想看某间房过去所有月份的收款明细，回 App 里的房间详情页看。

---

## 第 10 章 · 怎么把 App 装到手机上

> 账号和仓库**你已经建好了**，所以这一章很短。

### 10.1 你的信息（忘了就翻这里）

| 项目 | 值 |
|---|---|
| 仓库地址 | `https://github.com/XiaoHao-ly/my-ledger` |
| 将来的 App 网址 | `https://xiaohao-ly.github.io/my-ledger/` |
| 登录账号 | XiaoHao-ly |

### 10.2 需要做的事

**第 1 步：把程序传上去**（我来做，你只需要登录一次）

我会在你电脑上操作。如果弹出让你登录 GitHub 的窗口，
你输入账号密码登录即可。**这一步只需要做一次，以后都不用再登。**

**第 2 步：打开「网页服务」开关**（我带你做，你点几下）

```
浏览器打开 https://github.com/XiaoHao-ly/my-ledger
   → 点顶部的 Settings（设置）
   → 左边找到 Pages
   → Source 选 "Deploy from a branch"
   → Branch 选 "main"，右边选 "/ (root)"
   → 点 Save
   → 等 1~2 分钟
```

**第 3 步：在手机上装成 App**（你自己做）

```
1. 手机用 Chrome 或 Edge 浏览器打开：
   https://xiaohao-ly.github.io/my-ledger/

2. 等页面显示出来

3. 点右上角的「⋮」菜单

4. 选「安装应用」或「添加到主屏幕」

5. 桌面出现「房东记账」图标，点开就是 App
```

### 10.3 ⚠️ 三个常见的坑

**坑 1：用微信打开链接，装不了**

微信内置的浏览器**不支持**「添加到主屏幕」。
如果你把链接发到微信里点开，会发现怎么都找不到安装按钮。
→ **一定要用手机自带的浏览器、Chrome 或 Edge 打开。**

**坑 2：GitHub 偶尔打不开**

GitHub 在国内访问有时不稳定。如果网页打不开：
- 先试试切换 Wi-Fi / 移动数据
- 多试几次，或者换个时间段
- **备用方案**：同样的文件也可以放到 Cloudflare Pages（同样免费、
  同样能装成 App）。需要时告诉我，我带你换过去，**数据不用重新录**。

**坑 3：不要用「无痕模式」**

无痕模式下存的数据，关掉窗口就没了。**一定要用正常模式打开。**

### 10.4 怎么确认装成功了

装成功后应该是这样：
- 桌面上有一个图标，是你自己单独的图标（不是 Chrome 的图标）
- 点开之后**看不到网址栏**，看起来就像个正常的 App
- 从最近任务里切回来，还是停在原来的位置

---

## 第 11 章 · 开发步骤规划

> 一共 11 步。每一步做完，你都能在手机上看到实实在在的变化。
> **每一步都是可以停下来交付的**，不会出现「做了一半不能用」的情况。

| 步骤 | 做什么 | **做完你能看到什么** |
|---|---|---|
| **1** | 搭骨架 + 真的推上线 | 手机浏览器打开网址，能看到「房东记账」页面和几个示例房间（此时刷新会重置，正常） |
| **2** | 变成能装的 App | 浏览器菜单里出现「安装应用」；装到桌面后有自己的图标；开飞行模式也能打开 |
| **3** | 数据能存住了 + 建区域、建单间 | 建一个「城东小区3栋」，手动加一间 101，**刷新页面数据还在** |
| **4** | 批量建房 | 填 1~6 层、每层 4 户，一次生成 101~604 共 24 间房 |
| **5** | 房间详情编辑 | 点进 101，能改房号、租金、押金、填租客姓名电话和租期 |
| **6** | **★ 确认收租（核心）** | 把 101 登记为已租；到了收租日没确认就自动变红进「未交租」；点「确认已收」后变绿、算进收入 |
| **7** | 换租客 / 退租 / 续租 | 给 101 办退租、录新租客；张三的历史记录还在，没被覆盖 |
| **8** | 统计 | 看到今年实际收到多少、还有几间没收、空置少赚了多少，以及逐月明细 |
| **9** | 备份与恢复 | 导出一个文件；清空数据后能完整导回来 |
| **10** | **★ 导出 Excel 表格** | 导出压缩包，解压得到 `2026/城东小区3栋.xlsx`，用手机或电脑的 Excel 打开，里面有 12 个标签页 |
| **11** | 打磨（大字大按钮等） | 一个敢长期用的成品 |

**第 6 步是整个 App 的价值核心。** 做完第 6 步，这个 App 就已经能真正帮你记账了。

---

## 第 12 章 · 小白词典

> 文档里出现的所有技术词，都在这儿用一句话解释。

| 词 | 一句话解释 |
|---|---|
| **App** | 手机上能点开用的东西，比如微信、支付宝 |
| **PWA** | 一种「用网页做成的 App」。外观和真 App 一样，但不用去应用商店下载 |
| **浏览器** | 你手机上用来上网的那个东西，比如 Chrome、Edge、手机自带的「浏览器」 |
| **Chrome / Edge** | 两个常用的浏览器。装 App 建议用这两个 |
| **网址** | 你在浏览器地址栏输入的那串东西，比如 `https://...` |
| **托管** | 把「程序」放到网上的服务器上，让手机能通过网络打开它。**注意：你的记账数据不会被放上去** |
| **服务器** | 一台一直开着的电脑，用来给别人提供网页。你的数据**不需要**它 |
| **GitHub** | 一个免费的网站，用来放程序文件。**你的账号已经建好了** |
| **GitHub Pages** | GitHub 提供的免费功能，能把你的文件变成一个可以打开的网址 |
| **仓库** | GitHub 上放你文件的地方，就像电脑上的一个文件夹 |
| **IndexedDB** | 浏览器用来存数据的地方。你的记账数据就存在这里。**它在你手机的浏览器内部** |
| **缓存** | 浏览器偷偷存下来的一份副本，让你断网时也能打开 |
| **Service Worker** | 让网页能「断网也能用」的那个幕后小工。**它必须单独放一个文件** |
| **manifest** | 一个小文件，告诉手机「我是个 App，请把我的图标放到桌面上」 |
| **JSON** | 一种文件格式，能用文字完整记录你的数据。你的备份文件就是这种格式 |
| **Excel / .xlsx** | 微软的表格文件格式。打开就是表格，能改、能打印、能算公式 |
| **CSV** | 另一种表格格式，更简单、什么软件都能开，但没有格式（不能加粗、不能调列宽） |
| **标签页** | Excel 文件底部的那些小标签（1月、2月……）。点一下就切到那一页 |
| **压缩包 / ZIP** | 把很多文件「打包」成的一个文件，体积更小、也方便一次下载。解压后能拿到里面的文件夹结构 |
| **解压** | 把压缩包还原成里面的文件和文件夹。手机上：文件管理 → 长按压缩包 → 解压 |
| **快照** | 某个瞬间的完整记录。导出的 Excel 就是「导出那一刻」的快照，之后不会自己更新 |
| **备份** | 把你的数据整个复制一份存起来，万一出事能恢复 |
| **导出 / 导入** | 导出 = 把数据存成一个文件；导入 = 从这个文件把数据读回来 |
| **本地** | 就在你这台手机上，不经过网络 |
| **云端** | 别人的服务器上。**这个 App 不用云端** |
| **回收站** | 删掉的东西先放这儿，30 天内能捡回来 |
| **刷新页面** | 在浏览器里往下拉一下，或者点那个转圈的箭头。能拿到最新版本 |

---

## 第 13 章 · 数据安全铁律（给 AI 自己看的）

> 这一章是写给 AI 的工作守则。房东可以跳过。

**最高原则：让房东「算错账」和「丢数据」，是仅有的两个不可接受的后果。**
所有技术取舍都服从这一条。

### 12.1 数据层

1. **App 启动时立即申请持久化存储**（`navigator.storage.persist()`），
   防止浏览器在空间紧张时自动清掉数据。**必须在房东还没积累数据时就做。**
2. **金额一律转成「分」做整数运算**（`Math.round(元 × 100)`），
   求和、按天折算全部在整数域完成。**绝不让小数参与累加**，
   否则会出现 `¥12,345.670000000002` 这种让房东失去信任的数字。
3. **日期一律存字符串** `'2026-09-10'` / `'2026-09'`，
   用本地时间构造。**绝不用 `Date` 对象或 `toISOString()` 存库**——
   UTC 转换会让月初的账落到上个月。
4. **写入顺序：先写数据库，成功了再更新界面。**
   绝不允许「界面先变了、写库却失败」——那会让房东以为存上了。
5. **所有「读取—修改—写回」必须在同一个事务里完成。**
   批量建房必须用**单个事务**写 N 条，避免建到一半失败产生畸形数据。
6. **删除一律用软删除**（打标记 + 回收站），不做自动清理。
7. **不使用 localStorage 存业务数据**（只有 5MB 且容易被清），
   只用它做一个「这个 App 被用过」的标记。

### 12.2 防盗号 / 防误清

8. **交叉哨兵**：`localStorage` 里放一个「用过了」的标记。
   启动时如果发现**数据是空的、但标记还在** → 弹出醒目警告
   「数据可能被清除了，请从备份恢复」，而不是安静地给一个空白 App。
9. **手机时间异常检测**：如果发现当前时间比上次写入时间还早，
   提示「手机时间似乎不对，统计可能异常」。

### 12.3 备份与恢复

10. **导入前强制自动导出当前数据**，并让房东确认已保存，才执行覆盖。
    **这一步不可跳过。**
11. **备份文件必须带校验信息**：格式版本号、导出时间、各类数据条数。
    导入时逐项比对，**任何不一致都拒绝导入并说明原因，
    绝不「尽力导入」**——猜错就是静默的数据损坏。
12. **超过 30 天未备份，首页显示提醒条。** 房东不会自己记得备份。
13. **备份文件里存「格式版本号」**。将来若备份格式变了，
    导入旧文件时走升级逻辑；导入更新的文件则拒绝并提示先更新 App。

### 12.4 数据库结构变更

14. **IndexedDB 的迁移用 `switch` 级联穿透（不写 `break`）**，
    保证任意旧版本都能升到最新。
15. **永远不删除已有的数据表、永远不给已有的表改主键。**
    只能「新建表 + 拷数据」。这是 IndexedDB 的硬性限制。
16. **加字段不需要迁移**：读取时统一走一个补默认值的函数，
    老记录缺字段也不会崩。
17. **迁移代码必须包 try/catch**，任何异常都不能让升级流程中断
    （否则数据库会卡在旧版本打不开）。

### 12.5 计算正确性

18. **`calc.js` 必须只包含纯函数**（不碰界面、不碰数据库），
    这样才能用测试反复验证。
19. **必须有单元测试**，至少覆盖：跨月边界（月末/月初）、
    2 月 28/29 日、收租日 31 号被夹取、新租客当月入住、
    月中换租客、月中退租、按天折算取整、金额求和精度、
    统计起始月的下界、未来月份不计入空置损失。
    **「算错不可接受」唯一的工程手段就是这些测试。**

### 12.6 界面与体验

20. **破坏性操作全部二次确认，且文案要说清后果。**
    例如：「删除后这间房的 18 条收租记录也会从统计中消失，确定吗？」
21. **任何数据库异常都要弹「操作失败，数据未改变」**，
    绝不允许静默失败。
22. **基础字号 16~18px，按钮触控区不小于 44px。** 房东不是年轻人。
23. **零外部依赖、零网络请求**：不引 CDN、不引第三方库、不发任何统计埋点。
    这样既保证离线可用，也不存在「第三方坏了导致 App 打不开」的风险。

### 12.7 部署

24. 仓库名是纯 ASCII（`my-ledger`），但 **`manifest` 和 `sw.js` 里的路径
    必须全部用相对路径（`./`）**，因为 GitHub Pages 服务在子路径下。
25. **Service Worker 必须用 `Promise.allSettled` 逐个预缓存**，
    不能用 `addAll`——任何一个文件 404 都会导致整个离线功能装不上。
26. **Service Worker 的缓存策略：页面和脚本用「先联网、失败再读缓存」。**
    绝不用「先读缓存」——那样一旦忘记改版本号，房东会永远卡在旧版本上，
    而且没人会发现。
27. **Service Worker 绝不触碰 IndexedDB。** 数据只由页面写。
    这样「更新 App」永远不会碰到房东的数据。

### 12.8 Excel 导出（风险最高的模块，必须严格验证）

> 房东选了**真 Excel（.xlsx）**，而不是低风险的 CSV。
> .xlsx 本质上是一个 ZIP 包着几个 XML 文件，**写错一个字节就打不开**，
> 而 AI 无法在房东的真实手机上验证。所以这一块必须有最强的验证。

28. **Excel 生成器必须是纯 JS 手写，零外部依赖**（房东要求 App 零依赖）。
    但**验证时可以用工具**：在本地用 Node 跑生成器，产出一个真的 xlsx。
29. **必须用 `unzip` 拆开生成的 xlsx，逐项核对内部结构**：
    `[Content_Types].xml`、`_rels/.rels`、`xl/workbook.xml`、
    `xl/_rels/workbook.xml.rels`、`xl/worksheets/sheetN.xml` 是否齐全，
    XML 是否格式正确。
30. **必须用一个专业的 Excel 读取库把文件读回来验证**（如 SheetJS，**仅本地验证用**，
    绝不进 App）。读得回来，才说明 Excel 能打开。
31. **验证工具只是开发辅助，放在 `tools/` 目录且不被 App 引用。**
    App 本身必须保持零依赖、零网络请求。
32. **ZIP 写入用 store（不压缩）即可**，避免 deflate 的实现风险；
    Excel 文件本来就不大。**CRC32 必须正确计算**，否则 Excel 拒绝打开。
33. **中文文件名要清洗**：`\ / : * ? " < > |` 这些字符在文件名里非法，
    区域名里很可能出现。必须替换掉，否则导出会静默失败。
34. **第一版必须同时导出一份 CSV 作为保底**（即使房东没要）。
    理由：Excel 第一次打开失败时，房东不至于完全拿不到数据。
    **等 Excel 连续几次验证没问题后，再问房东要不要去掉 CSV。**
35. **导出前必须校验数据完整性**，导出后**显示导出了多少条、多少个文件**，
    让房东能核对数字对不对。

---

## 附：和房东的沟通备忘录

**房东的特点**（沟通时请记住）：
- 完全不会编程，也不打算学
- 能熟练用手机，但不懂技术名词
- 关心的是「我的账对不对」「我的数据会不会丢」
- 提需求时用的是生活语言（「空房的未收入」），需要翻译成技术方案
- **会主动纠正自己的决定**（比如从「按该收的算」改成「按实收算」），
  这是好事，说明他在认真想。**改需求时要先改文档，再改代码。**

**已经发生过的需求变更**（记录在案，避免反复）：
1. 收入口径：从「按应收算」→ 改为「**按实收算**」（只有确认收租才计入收入）
2. 图片功能：从「每房最多 9 张」→ 改为「**完全不做**」
3. 空置损失：确认保留，但必须**单独显示**，不混进收入
4. **新增 Excel 导出**：房东要求「不通过 App 也能看到数据」，
   且要能用表格保存。经确认：
   - 格式选**真 Excel（.xlsx）**（AI 曾建议低风险的 CSV，房东仍选 Excel）
   - 组织方式：**每年每区域一个文件**，内含 **12 个标签页**（1月~12月）
   - 交付方式：打包成**压缩包**，解压后得到 `年份/区域.xlsx`
   - 列：房号、区域、月租、押金、租客、电话、入住时间、退租时间、
     本月状态、本月实收、收款日期
   - **重要澄清**：表格是「导出/复印件」，**不是 App 的存储**。
     App 内部的账本仍然是 IndexedDB。
5. **补录以前月份（2026-09-10 房东报的 bug）**：房东 9 月要录入一个 7月28日入住的租客，
   结果补记时把 9 月顶成了「已收」。经确认：
   - 房东要的是**「登记时自动弹一张待补录清单」**，不是「自己回房间详情页慢慢找」
   - 首月规则：**7月28日入住当天就收 7 月的租金**（7月算一期，往后每月28日一期）
   - 点「没收到」**什么都不记**，下次打开清单还会列出来
   - 房东**主动问过要不要做「能精确到分钟」的测试环境**。结论：**不做**。
     因为 `roomMonthState` 只拿「今天」和「收租日」比大小，
     根本没有「几点几分」这个维度，设 18:10 和 00:00 结果完全一样——测了等于没测，
     反而会让人误以为「验过了」。改用在电脑上跑**天的边界**自动测试
     （27号不提醒 / 28号起直接算未收）。
   - 附带发现的坑：`statsStartMonth` 存了值但**代码里从没读过**（见第 7.4 节那行）。
     本次**故意不启用**——一启用，房东补录 7、8 月就会被它挡住。留待单独决定。
6. **收租日搬到登记租客的表单里 + 删掉「今天该收租」（2026-09-10）**：
   房东说「录入租户信息时没有选择收租日期，我想有个输入的框」。
   - AI 起初提的方案是：给**租约**加一个 `rentDueDay` 字段，默认值按「入住日的前一天」
     推算，还要处理月初/月末的特殊情况。
   - 房东**主动叫停**：「简单一点吧……俩个用同一个就行，不需要别的多余的改动，
     也不需要计算天数，就为默认的收租日期就行」
   - 最终做法：**就复用房间上那个 `room.rentDueDay`**，表单里再放一个入口，
     默认显示房间现在的值。**不做任何推算。** 数据模型一个字没动。
   - 同时房东要求删掉「🟡 今天该收租」这个状态：「有点多余，到了交租日期当天以及之后，
     状态就应该换成未收租，并放到未收租的列表中」
   - **教训**：房东要的常常只是「多个入口」，不是「一套新规则」。
     下次提方案之前，先问一句「你是想要一个新的算法，还是只想把现有的框再放一份？」

**沟通时的一个提醒**：房东会把「我想要数据能以表格形式保存」
和「数据存哪」混在一起问。**每次都要先澄清「账本」和「复印件」的区别**，
再谈技术方案。参见第 5.2 节和第 9.1 节。

**每次开始工作前**：先读第 1 章，再读第 12 章。
