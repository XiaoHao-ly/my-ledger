# AI 工具的本地配置，不属于 App 内容，不上传
.claude/

# 系统生成的杂项文件
.DS_Store
Thumbs.db
desktop.ini

# 测试工具（fake-indexeddb 模拟手机数据库、SheetJS 用来验证生成的 Excel）。
# 只在电脑上跑测试时用，不进 App、也不上传到网上。
# App 本身必须保持零依赖 —— 依赖清单见 package.json。
node_modules/

# 临时文件
*.log
*.tmp
tmp/
