# AI 工具的本地配置，不属于 App 内容，不上传
.claude/

# 系统生成的杂项文件
.DS_Store
Thumbs.db
desktop.ini

# 第 10 步做 Excel 导出时，本地装来"验证"用的工具。
# 只在我的电脑上跑，不进 App，也不上传（App 本身必须零依赖）。
node_modules/
package-lock.json

# 临时文件
*.log
*.tmp
